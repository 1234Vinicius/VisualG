Algoritmo "Vetores"

Var
nomes: vetor[0..9] de caractere
posicao:inteiro


Inicio
para posicao de 0 ate 9 faca
escreval("Nome do vendedor: ")
leia(nomes[posicao])

fimpara

para posicao de 0 ate 9 faca
escreval("A posicao ", posicao, "do vetor, contém o nome: ", nomes[posicao])
fimpara


Fimalgoritmo